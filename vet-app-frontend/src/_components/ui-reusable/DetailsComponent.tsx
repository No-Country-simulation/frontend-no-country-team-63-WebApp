"use client"
import { IRecordatorios } from "@/types/recordatorios";

const DetailsComponent = (
  // { data }: { data: IRecordatorios }
) => {
  return (
    <div>
      <p>Detalles</p>
      {/* {data.nombreEvento} */}
    </div>
  );
};

export default DetailsComponent;
